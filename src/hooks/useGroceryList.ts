import {useEffect,useState} from 'react'; import {GroceryItem} from '../types/grocery'; import {groceryService} from '../services/groceryService'; export function useGroceryList(){const [items,setItems]=useState<GroceryItem[]>([]);useEffect(()=>{groceryService.list().then(setItems)},[]);const toggle=(id:string)=>setItems(xs=>xs.map(x=>x.id===id?{...x,checked:!x.checked}:x));return {items,toggle}};
