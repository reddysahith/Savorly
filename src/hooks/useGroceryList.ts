import {useEffect,useState} from 'react'; import {GroceryItem} from '../types/grocery'; import {groceryService} from '../services/groceryService'; export function useGroceryList(){const [items,setItems]=useState<GroceryItem[]>([]);useEffect(()=>{groceryService.list().then(setItems)},[]);const toggle=async (id:string)=>{const item = items.find(x => x.id === id); if(!item) return; setItems(xs=>xs.map(x=>x.id===id?{...x,checked:!x.checked}:x)); await groceryService.toggle(id, !item.checked);};return {items,toggle}};
