import {GroceryItem} from '../../types/grocery'; import {GroceryItemRow} from './GroceryItemRow'; export function AisleGroup({aisle,items,onToggle}:{aisle:string;items:GroceryItem[];onToggle:(id:string)=>void}){return <section className="aisle"><h3><span>{aisle}</span><span>{items.length} items</span></h3>{items.map(i=><GroceryItemRow key={i.id} item={i} onToggle={()=>onToggle(i.id)}/>)}</section>}
