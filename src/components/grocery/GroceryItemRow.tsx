import {ChevronDown,ChevronUp,Check} from 'lucide-react'; import {useState} from 'react'; import {GroceryItem} from '../../types/grocery'; import {ItemProvenanceBadge} from './ItemProvenanceBadge'; export function GroceryItemRow({item,onToggle}:{item:GroceryItem;onToggle:()=>void}){const [open,setOpen]=useState(false);return <div className={`grocery-row ${item.checked?'completed':''}`}><button className={`check ${item.checked?'done':''}`} onClick={onToggle} aria-label={`Mark ${item.name} ${item.checked?'not ':''}complete`}>{item.checked&&<Check size={14}/>}</button><div className="grocery-info"><strong>{item.name}</strong><small>{item.quantity} {item.unit} · <ItemProvenanceBadge count={item.sourceRecipes.length}/></small>{open&&<div style={{marginTop:8,color:'#747a72',fontSize:12}}>From: {item.sourceRecipes.join(' · ')}</div>}</div><button className="close" style={{background:'transparent'}} onClick={()=>setOpen(!open)} aria-label="Show provenance">{open?<ChevronUp size={16}/>:<ChevronDown size={16}/>}</button></div>}
