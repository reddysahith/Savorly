import {CalendarDays,Home,LibraryBig,ShoppingBasket} from 'lucide-react'; export function MobileNav({view,setView}:{view:string;setView:(v:string)=>void}){return <nav className="mobile-nav">{[['home','Home',Home],['recipes','Recipes',LibraryBig],['plan','Plan',CalendarDays],['shop','Shop',ShoppingBasket]].map(([id,l,I]:any)=><button className={view===id?'active':''} onClick={()=>setView(id)} key={id}><I size={19}/>{l}</button>)}</nav>}
