import {Timer} from 'lucide-react'; import {useEffect,useState} from 'react'; export function TimerChip({seconds}:{seconds:number}){const [left,setLeft]=useState(seconds),[active,setActive]=useState(false);useEffect(()=>{if(!active)return;const x=setInterval(()=>setLeft(v=>{if(v<=1){clearInterval(x);return 0}return v-1}),1000);return()=>clearInterval(x)},[active]);return <button className="timer" onClick={()=>setActive(!active)}><Timer size={15}/>{left?`${Math.floor(left/60)}:${String(left%60).padStart(2,'0')}`:'Done'} {active?'Pause':'Start'}</button>}
