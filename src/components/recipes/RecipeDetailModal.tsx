import {X,Play,Plus,ChefHat} from 'lucide-react'; import {Recipe} from '../../types/recipe'; import {ServingScaler} from './ServingScaler'; import {Button} from '../common/Button'; export function RecipeDetailModal({recipe,onClose,onCook}:{recipe:Recipe;onClose:()=>void;onCook:()=>void}){return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><div><div className="eyebrow">{recipe.visibility} recipe</div><h2>{recipe.title}</h2><p className="subtitle">{recipe.description}</p></div><button className="close" onClick={onClose} aria-label="Close"><X size={18}/></button></div><div className="detail-hero"><div className="detail-icon" style={{background:recipe.coverColor}}><ChefHat size={42} strokeWidth={1.6}/></div><div><strong>By {recipe.author}</strong><div className="meta">{Math.round((recipe.prepSeconds+recipe.cookSeconds)/60)} min total · {recipe.yieldValue} servings</div></div></div><div className="tabs"><button className="active">Ingredients</button><button>Method</button></div><ServingScaler recipe={recipe}/><div style={{display:'flex',gap:10,marginTop:24}}><Button className="primary" onClick={onCook}><Play size={16}/> Start cooking</Button><Button><Plus size={16}/> Add to plan</Button></div></div></div>}
