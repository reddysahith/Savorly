import {Clock,Lock,Users,ChefHat} from 'lucide-react'; import {Recipe} from '../../types/recipe'; export function RecipeCard({recipe,onClick}:{recipe:Recipe;onClick:()=>void}){return <button className="card recipe-card" onClick={onClick} style={{textAlign:'left',border:0}}><div className="recipe-cover" style={{background:recipe.coverColor}}><ChefHat size={54} strokeWidth={1.6}/></div><div className="recipe-body"><h3>{recipe.title}</h3><div className="meta"><Clock size={13} style={{verticalAlign:'middle'}}/> {Math.round((recipe.prepSeconds+recipe.cookSeconds)/60)} min · {recipe.author}</div><div className="tags">{recipe.tags.map(t=><span className="tag" key={t}>{t}</span>)}<span className="tag">{recipe.visibility==='private'?<Lock size={12}/>:<Users size={12}/>} {recipe.visibility}</span></div></div></button>}
