import {Minus,Plus} from 'lucide-react'; import {Recipe} from '../../types/recipe'; import {useState} from 'react'; export function ServingScaler({recipe}:{recipe:Recipe}){const [servings,setServings]=useState(recipe.yieldValue);const ratio=servings/recipe.yieldValue;return <div><div className="scale"><span className="meta">Servings</span><button aria-label="Decrease servings" onClick={()=>setServings(Math.max(1,servings-1))}><Minus size={15}/></button><strong>{servings}</strong><button aria-label="Increase servings" onClick={()=>setServings(servings+1)}><Plus size={15}/></button></div><div style={{marginTop:14}}>{recipe.ingredients.map(i=><div className="ingredient" key={i.id}><span>{i.ingredientName}</span><strong>{Number((i.quantity*ratio).toFixed(2))||''} {i.unit}</strong></div>)}</div></div>}
