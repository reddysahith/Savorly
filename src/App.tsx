import {useState} from 'react'; import {ArrowRight,CheckCircle2,Sparkles} from 'lucide-react'; import {Sidebar} from './components/layout/Sidebar'; import {Topbar} from './components/layout/Topbar'; import {MobileNav} from './components/layout/MobileNav'; import {HeroTodayCard} from './components/dashboard/HeroTodayCard'; import {UpcomingList} from './components/dashboard/UpcomingList'; import {RecipeCard} from './components/recipes/RecipeCard'; import {RecipeDetailModal} from './components/recipes/RecipeDetailModal'; import {RecipeImportModal} from './components/recipes/RecipeImportModal'; import {WeekGrid} from './components/planner/WeekGrid'; import {AiPlanDrawer} from './components/planner/AiPlanDrawer'; import {AisleGroup} from './components/grocery/AisleGroup'; import {CookModeModal} from './components/cook/CookModeModal'; import {useRecipes} from './hooks/useRecipes'; import {usePlanner} from './hooks/usePlanner'; import {useGroceryList} from './hooks/useGroceryList'; import {Recipe} from './types/recipe'; import './styles/tokens.css';
function Home({onCook,onRecipe}:{onCook:()=>void;onRecipe:(r:Recipe)=>void}){const {recipes}=useRecipes();return <div className="page"><div className="title-row"><div><div className="eyebrow">Tuesday, September 22</div><h1 className="title">Good morning, Maya</h1><p className="subtitle">A little planning now, a calmer week later.</p></div><div className="tag"><CheckCircle2 size={14}/> Household is in sync</div></div><div className="grid grid-2"><HeroTodayCard onCook={onCook}/><UpcomingList/></div><div className="section-head"><h2>Ideas for this week</h2><button className="btn ghost">See all <ArrowRight size={15}/></button></div><div className="grid grid-3">{recipes.map(r=><RecipeCard key={r.id} recipe={r} onClick={()=>onRecipe(r)}/>)}</div></div>}
function Recipes({onRecipe,onAdd}:{onRecipe:(r:Recipe)=>void;onAdd:()=>void}){const {recipes}=useRecipes();return <div className="page"><div className="title-row"><div><div className="eyebrow">Your library</div><h1 className="title">Recipes worth repeating</h1><p className="subtitle">3 recipes · synced with your household</p></div><button className="btn primary" onClick={onAdd}>+ Add recipe</button></div><div className="list-page"><aside className="filters"><input className="search" placeholder="Search library"/><button className="filter selected">All recipes <span style={{float:'right'}}>3</span></button><button className="filter">Mine</button><button className="filter">Household</button><button className="filter">Saved</button><div className="mini" style={{marginTop:18}}>Filter by</div><button className="filter">Vegetarian</button><button className="filter">Under 30 min</button></aside><div className="recipe-grid">{recipes.map(r=><RecipeCard key={r.id} recipe={r} onClick={()=>onRecipe(r)}/>)}</div></div></div>}
function Plan({onAi}:{onAi:()=>void}){const {plan}=usePlanner();return <div className="page"><div className="title-row"><div><div className="eyebrow">Household planning</div><h1 className="title">The week ahead</h1><p className="subtitle">September 21 – 27 · 3 meals planned</p></div><button className="btn primary" onClick={onAi}><Sparkles size={17}/> Suggest a plan</button></div><div className="card" style={{padding:16}}><WeekGrid plan={plan}/></div><div className="section-head"><h2>Planning notes</h2></div><div className="card"><p style={{margin:0}}>Thursday is looking open. Try a quick pantry dinner or add a leftover night.</p></div></div>}
function Shop(){const {items,toggle}=useGroceryList();const aisles=['Produce','Pantry','Dairy','Spices','Bakery'];const done=items.filter(i=>i.checked).length;return <div className="page"><div className="title-row"><div><div className="eyebrow">Sunday Supper Club</div><h1 className="title">Fresh market list</h1><p className="subtitle">7 items · synced just now</p></div><button className="btn">+ Add item</button></div><div className="grocery-summary"><div className="ring"/><div><strong>{done} of {items.length} items checked</strong><div className="meta">Nice progress — keep going.</div></div></div>{aisles.map(a=>{const xs=items.filter(i=>i.aisle===a&&!i.checked);return xs.length?<AisleGroup key={a} aisle={a} items={xs} onToggle={toggle}/>:null})}<div className="section-head"><h2>Completed <span className="tag">{done}</span></h2></div>{items.filter(i=>i.checked).map(i=><AisleGroup key={i.id} aisle="" items={[i]} onToggle={toggle}/>)}</div>}
export default function App(){const [view,setView]=useState('home'),[selected,setSelected]=useState<Recipe|null>(null),[importing,setImporting]=useState(false),[cook,setCook]=useState<Recipe|null>(null),[ai,setAi]=useState(false);const active=selected??cook;return <div className="app"><Sidebar view={view} setView={setView}/><main className="main"><Topbar onAdd={()=>setImporting(true)}/>{view==='home'&&<Home onCook={()=>setCook({id:'r2',title:'Miso butter salmon',description:'',yieldValue:2,prepSeconds:0,cookSeconds:0,visibility:'private',tags:[],author:'Savorly kitchen',coverColor:'#f6dfd3',ingredients:[],instructions:[{position:1,body:'Whisk miso, butter and honey together.'},{position:2,body:'Roast salmon for 14 minutes.',durationSeconds:840},{position:3,body:'Serve with rice and greens.'}]})} onRecipe={setSelected}/>} {view==='recipes'&&<Recipes onRecipe={setSelected} onAdd={()=>setImporting(true)}/>} {view==='plan'&&<Plan onAi={()=>setAi(true)}/>} {view==='shop'&&<Shop/>}{view==='discover'&&<div className="page"><div className="eyebrow">A little inspiration</div><h1 className="title">Discover recipes</h1><p className="subtitle">Fresh ideas from cooks in the Savorly community.</p></div>}<MobileNav view={view} setView={setView}/></main>{selected&&<RecipeDetailModal recipe={selected} onClose={()=>setSelected(null)} onCook={()=>{setCook(selected);setSelected(null)}}/>}{importing&&<RecipeImportModal onClose={()=>setImporting(false)} onSaved={()=>setImporting(false)}/>} {cook&&!selected&&<CookModeModal recipe={cook} onClose={()=>setCook(null)}/>} {ai&&<AiPlanDrawer onClose={()=>setAi(false)}/>}</div>}
